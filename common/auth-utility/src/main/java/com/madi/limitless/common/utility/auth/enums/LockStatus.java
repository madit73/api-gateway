package com.madi.limitless.common.utility.auth.enums;


/**
 * Indicates whether a user account is locked for authentication.
 */
public enum LockStatus
{
    /** Account is locked and cannot authenticate. */
    LOCKED,
    /** Account is unlocked and eligible to authenticate. */
    UNLOCKED
}
