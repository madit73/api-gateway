package com.madi.common.utility.auth.enums;


/**
 * High-level role categories used for authorization decisions.
 */
public enum RoleType
{
    /** Standard end-user permissions. */
    USER,
    /** Administrative permissions with elevated access. */
    ADMIN
}
